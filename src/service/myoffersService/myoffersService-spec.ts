import {MyoffersService} from './myoffersService';

describe('myoffersService', function() {

  beforeEach(angular.mock.module('main'));

  it('should ...', inject(function(myoffersService:MyoffersService) {

    //expect(myoffersService.doSomething()).toEqual('something');

  }));

});
